apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service-app
  labels:
    app: user-service-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service-app
  template:
    metadata:
      labels:
        app: user-service-app
    spec:
      containers:
        - name: user-service-app
          image: rohitaggarwal88/user-service:0.0.2-SNAPSHOT
          imagePullPolicy: Always
          ports:
            - containerPort: 6002

---
apiVersion: v1
kind: Service
metadata:
  name: user-service-svc
spec:
  ports:
    - targetPort: 6002
      port: 80
  selector:
    app: user-service-app